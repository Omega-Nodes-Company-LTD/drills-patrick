node_modules
.next
.git
.github
e2e
playwright-report
test-results
coverage
*.md
!README.md
.env
.env.local
.env*.local

# `Dockerfile` must NOT be listed here. BuildKit resolves the Dockerfile
# through the build context and honours these patterns while doing so, so
# ignoring it makes the build fail with
# "failed to read dockerfile: open Dockerfile: no such file or directory".
docker-compose.yml
